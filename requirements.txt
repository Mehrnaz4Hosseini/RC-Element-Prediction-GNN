# RC-Element-Prediction-GNN dependencies
# Install (Windows, GPU users pick their own torch build):
#   py -3.12 -m venv .venv
#   .venv\Scripts\python -m pip install -r requirements.txt
# For a CPU-only torch:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# Core deep-learning / graph stack
torch>=2.4
torch-geometric>=2.6

# Scientific / data
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
networkx>=3.0

# IO / config
pyyaml>=6.0
openpyxl>=3.1

# Visualization
matplotlib>=3.7
seaborn>=0.13
plotly>=5.0

# Notebook execution
ipykernel>=6.0
